import { PrefCurrencyDTO } from "types/api/prefCurrencyAPI";
import { Currency } from "types/currency";

export const toPrefCurrency = (prefCuyrrency: PrefCurrencyDTO): Currency => {
  return prefCuyrrency;
};
